# Live mode: the browser uses the same-origin gateway; only the server knows the upstream.
NEXT_PUBLIC_API_URL=/api/v1
RECORDSHIELD_BACKEND_URL=https://34-237-67-194.sslip.io/api/v1

# Mock mode: set BOTH values to empty, then restart/rebuild Next.js.
# Never use the remote URL as NEXT_PUBLIC_API_URL: cookies need the same-origin gateway.
