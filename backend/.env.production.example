# Copied to .env.production on the server. Every value must be a fresh random secret:
#   openssl rand -hex 32
DB_PASSWORD=
DB_ROOT_PASSWORD=
SESSION_SECRET=
M1_TEST_KEY=
AUDIT_SERVICE_KEY=
MERCY_EMR_SERVICE_KEY=

# Public hostname Caddy obtains a Let's Encrypt certificate for (a domain you own, or <ip-with-dashes>.sslip.io)
SITE_ADDRESS=
